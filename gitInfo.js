/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

const gitDefinition = "Git is a version control system that allows individuals and teams to track changes to code projects, prevention of bugs and loss of programs and allows you to view the long history of a project in snapshots. In layman terms, git is a software system used alongside github to manage projects."

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

const gitHubDefintion = "GitHub is a cloud-based git repository aka filing system that provides a resource for developers to collaborate and manage projects."

//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

const gitInitDefintiion = "Git Init is a Git command that initializes a local repository in directed folder. In layman terms, Git Init opens an access to conenct your computer's file to a file in Github."

//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

const gitCloneDefinition = "Git Clone is a Git command that copies an already existing Git repository. How teams pull repository into their own computer."

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

const gitStatusDefinition = "Git Status is a git command that displays the state of the working directory and the staging area. Essentially, this command allows you to see documented changes, undocumented changes and which files are being tracked by Git."

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

const gitAddDefinition = "Git Add is a Git command that adds some files to be tracked. Essentially, Git Add places your computer's file in the Git repository."

//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

const gitCommitDefiniton = "Git commit is a Git command that creates a snap or checkpoint of the files I'm tracking. In layman terms, Git Commit solidifies the saved version of your file before transferring it into the Git repository."

const gitCommitCode = "Git Commit Code are the command lines to transfer and save your computer's file into the Git repository. git commit -m 'message goes here'"


//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

const gitPush = "Git Push is a Git command that finalizes in uploading your computer's file including saved versions/commits into the Git repository."

// Analogy - I like to think of git as getting a present for someone. git add is when you buy the present, git commit is when you wrap the present, and git push is when you give the present to your friend.